services:
  discord-overlord:
    image: ghcr.io/YOUR_GITHUB_USERNAME/discord-overlord:latest
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    env_file: .env
